version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

  # Можно добавить базу данных, Redis и т.д.
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: mini_app
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"


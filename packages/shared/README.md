# Shared Package

Общий пакет с утилитами, типами и константами для использования во всех частях приложения.

## Использование

```typescript
import { generateId, formatDate, BaseEntity, API_BASE_URL } from 'shared';

const id = generateId();
const dateStr = formatDate(new Date());
```

